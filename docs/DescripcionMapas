OBSOLETO!!! SE DEBE ACTUALIZAR


# Las lineas que empiezan por "#" son comentarios, serán ignoradas por el programa.
# Todas las líneas tendrán una estructura determinada.
#    palabra_clave = valor
# El programa buscará esta palabra clave e intentara leer el valor y guardarlo en memoria.
# A continuación se listan los tipos de campos.


# Lines beginning with the character "#" are remarks. They will be ignored by the program.
# All the lines will have this structure.
#     keyword = value
# This program will searth for a keyword and will try to read the value and store it into memory.
# The keyword list can be found below.



# Nombre del mapa
# Map name
nombre=Nombre del mapa



# Numero de segmentos
# Number of line segments
num_segmentos=27



# Modo de giro de mapa. indica respecto a que punto gira el mapa 
   (0=camara, 1=punto_fijo)
# Map rotating mode, determines the point the map will rotate around.
   (0=camera, 1=fixed point)
modo_giro_mapa=1



# Posicion del punto de giro del mapa
# La siguientes linea son las coordenadas del punto de giro.
# Unicamente se tiene en cuenta para el caso de giro alrededor de un punto fijo
# Fixed point rotating point. This line indicates rotating point coordinates, only in the case the map rotates around a fixed point.
punto_giro=(416,355)


# ;ME LLEGO AQUIIIIIII

# Las siguientes linea no comentada es el limites de giro del mapa (angulo maximo y angulo minimo con el signo contrario)
# Nota: el origen es arriba
# Nota: la unidad son grados
30

# Posicion inicial de la moneda
0
300

# Gravedad (aceleracion hacia abajo)
100


# A partir de este punto nos encontraremos los segmentos uno a uno, con la siguiente estructura repetida.
# Punto inicial, coordenada X (double)
# Punto inicial, coordenada Y (double)
# Punto final, coordenada X (double)
# Punto final, coordenada Y (double)
# Tipo de segmento (0=wall, 1=goal, 2=death)
25
13
43
72
0




# Nota: ara hacer esto bien, esto pide un formato XML con etiquetas
Nota: ver "minixml", "libxml", "expat", "xerces", etc.
